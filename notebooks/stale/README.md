# Parked notebooks

Parked during the 0.4 cleanup and **not maintained against the current
API** (they still use the removed `terms=` keyword and node-level
`transform=`). Restore one by moving it back up and migrating it to the
transformation syntax; the maintained examples are
[`intro_tram_dag.py`](../intro_tram_dag.py) and
[`demo_tram_dag_colab.py`](../demo_tram_dag_colab.py).
